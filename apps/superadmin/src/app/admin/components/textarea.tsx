import React from 'react';

const MyTextAreaComponent: React.FC = () => (
  <textarea
    style={{ background: 'lightgreen', color: 'darkblue' }}
    placeholder="email"
  />
);

export default MyTextAreaComponent;
